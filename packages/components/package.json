{
  "name": "components",
  "version": "12.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "echo build",
    "release.major": "npm version major -m 'Release major version %s'",
    "preversion": "npm run build",
    "version": "",
    "postversion": "cat package.json && git log -3 && git add . && git commit -m 'from post release %s' && git push && git checkout main && git push --tags"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mapsindoors/midt": "^18.1.7"
  }
}

{
  "name": "components",
  "version": "19.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "echo build",
    "release.major": "NEW_VERSION=$(npm version major) && git add package.json package-lock.json && git commit -m 'Release version' && echo 'Bumped version to $NEW_VERSION' && git tag $NEW_VERSION && echo 'Tagged with $NEW_VERSION' && git push && git push --tags",
    "release": "sh ./release.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mapsindoors/midt": "^18.1.7"
  }
}

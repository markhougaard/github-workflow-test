{
  "name": "components",
  "version": "15.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "echo build",
    "release.major": "npm --tag-version-prefix='components-v' version major -m 'Release major version %s'",
    "preversion": "npm run build",
    "version": "",
    "postversion": "cat package.json && git log -3 && git tag -l && git push --follow-tags origin main"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mapsindoors/midt": "^18.1.7"
  }
}
{
  "name": "components",
  "version": "14.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "echo build",
    "release.major": "npm version major -m 'Release major version %s' --no-git-tag-version",
    "preversion": "npm run build",
    "version": "git add . && git commit -m 'Release major version'",
    "postversion": "cat package.json && git log -3 && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mapsindoors/midt": "^18.1.7"
  }
}

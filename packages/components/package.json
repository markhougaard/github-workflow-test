{
  "name": "components",
  "version": "15.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "echo build",
    "release.major": "npm --tag-version-prefix='components-v' version major -m 'Release major version %s'",
    "preversion": "npm run build",
    "version": "",
    "postversion": "cat package.json && git log -3 && echo 'tags list:' && git tag -l && echo 'trying to do git push now' && git push --follow-tags origin main"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mapsindoors/midt": "^18.1.7"
  }
}
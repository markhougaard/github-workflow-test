{
  "name": "components",
  "version": "16.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "echo build",
    "release.major": "NEW_VERSION=$(npm version major) && git add package.json package-lock.json && git commit -m 'Release version' && git tag $NEW_VERSION && echo 'Bumped version to $NEW_VERSION'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mapsindoors/midt": "^18.1.7"
  }
}